# ChurchHub - AI Agents

## Project Overview
ChurchHub (Ministrify) - система управління церквою. Laravel 10 + Blade + Alpine.js + Tailwind CSS.

---

## АВТОМАТИЧНИЙ РЕЖИМ

Коли користувач дає будь-яке завдання, АВТОМАТИЧНО запускай відповідних агентів паралельно:

### При отриманні завдання на нову фічу:
1. **Planning Agent** - спланувати реалізацію
2. **Security Agent** - перевірити безпеку підходу
3. **UX Agent** - продумати користувацький досвід

### При завданні виправити баг:
1. **Bug Hunter** - знайти пов'язані проблеми
2. **Code Review** - перевірити якість фіксу

### При завданні "перевір проект" / "що потрібно зробити":
1. **Status Agent** - стан проекту
2. **Bug Hunter** - потенційні баги
3. **UX Agent** - UX проблеми
4. **Security Agent** - вразливості
5. **Optimize Agent** - продуктивність

### При завданні перед релізом / деплоєм:
1. **Deploy Agent** - чекліст
2. **Security Agent** - аудит
3. **Bug Hunter** - критичні баги

### Формат автоматичної відповіді:

```markdown
## Аналіз завдання

Запускаю агентів паралельно...

---

### 🔍 Code Review
[результат]

### 🎨 UX Analysis
[результат]

### 🔒 Security Check
[результат]

### 🐛 Bug Scan
[результат]

---

## Рекомендації
1. Пріоритетна дія
2. ...

## План дій
- [ ] Крок 1
- [ ] Крок 2
```

---

---

## Agent: /review - Code Review

При виклику `/review` або "зроби рев'ю":

1. **Перевір останні зміни:**
   ```bash
   git diff HEAD~5 --stat
   ```

2. **Аналізуй на:**
   - Безпека (SQL injection, XSS, CSRF)
   - Продуктивність (N+1 queries, missing indexes)
   - Чистота коду (DRY, SOLID)
   - Консистентність стилів

3. **Формат відповіді:**
   ```
   ## Code Review Report

   ### Security
   - [ ] Issue or ✅ OK

   ### Performance
   - [ ] Issue or ✅ OK

   ### Code Quality
   - [ ] Issue or ✅ OK

   ### Recommendations
   1. ...
   ```

---

## Agent: /test - Testing Agent

При виклику `/test` або "протестуй":

1. **Запусти тести:**
   ```bash
   php artisan test
   ```

2. **Перевір покриття:**
   - Які контролери не мають тестів
   - Які критичні функції не протестовані

3. **Запропонуй тести** для непокритого коду

---

## Agent: /ux - UX Review Agent

При виклику `/ux` або "перевір UX":

1. **Аналізуй views:**
   - Консистентність UI компонентів
   - Mobile responsiveness
   - Доступність (a11y)
   - Темна тема

2. **Перевір:**
   - Розмір touch targets (min 44px)
   - Контраст кольорів
   - Валідація форм
   - Empty states
   - Loading states

3. **Формат:**
   ```
   ## UX Audit Report

   ### Critical Issues
   ### Warnings
   ### Suggestions
   ```

---

## Agent: /plan - Feature Planning

При виклику `/plan [feature]`:

1. **Досліди існуючий код** пов'язаний з фічею
2. **Створи план:**
   - Database changes
   - Backend (models, controllers, services)
   - Frontend (views, components)
   - Tests
3. **Оціни складність:** Low/Medium/High

---

## Agent: /bugs - Bug Hunter

При виклику `/bugs`:

1. **Шукай потенційні баги:**
   ```bash
   grep -r "TODO\|FIXME\|BUG\|HACK" app/
   ```

2. **Аналізуй:**
   - Необроблені exceptions
   - Missing null checks
   - Race conditions
   - Edge cases

3. **Перевір логи:**
   ```bash
   tail -100 storage/logs/laravel.log
   ```

---

## Agent: /status - Project Status

При виклику `/status`:

1. **Git status:**
   ```bash
   git status
   git log --oneline -10
   ```

2. **Database:**
   ```bash
   php artisan migrate:status
   ```

3. **Dependencies:**
   ```bash
   composer outdated
   npm outdated
   ```

4. **Створи звіт** про стан проекту

---

## Agent: /optimize - Performance Optimizer

При виклику `/optimize`:

1. **Знайди N+1 queries** в контролерах
2. **Перевір індекси** в міграціях
3. **Аналізуй:**
   - Великі запити без пагінації
   - Missing eager loading
   - Inefficient loops

---

## Agent: /security - Security Audit

При виклику `/security`:

1. **Перевір:**
   - Всі форми мають @csrf
   - Авторизація в контролерах
   - Валідація input даних
   - Sanitization output
   - Sensitive data exposure

2. **Скануй на вразливості:**
   - SQL injection points
   - XSS vectors
   - Mass assignment
   - Insecure direct object references

---

## Quick Commands

| Команда | Опис |
|---------|------|
| `/review` | Code review останніх змін |
| `/test` | Запустити тести |
| `/ux` | UX/UI аудит |
| `/plan [feature]` | Спланувати фічу |
| `/bugs` | Знайти потенційні баги |
| `/status` | Статус проекту |
| `/optimize` | Оптимізація продуктивності |
| `/security` | Security аудит |
| `/deploy` | Чекліст перед деплоєм |

---

## Project Structure

```
app/
├── Http/Controllers/     # Контролери
├── Models/               # Eloquent моделі
├── Services/             # Бізнес логіка
├── Traits/               # Reusable traits
resources/views/
├── layouts/              # Layouts (app, guest, public)
├── components/           # Blade components
├── [feature]/            # Feature views
```

## Key Models
- Church - головна сутність, все прив'язано до церкви
- Person - члени церкви
- User - користувачі системи (login)
- Event - події/служіння
- Ministry - служіння
- Group - малі групи

## Important Patterns
- Multi-tenancy через church_id
- Trait RequiresChurch для контролерів
- Trait Auditable для логування змін
- Alpine.js для інтерактивності
- Tailwind CSS для стилів
